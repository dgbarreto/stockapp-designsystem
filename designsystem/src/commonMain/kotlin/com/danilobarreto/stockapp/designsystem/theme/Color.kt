package com.danilobarreto.stockapp.designsystem.theme

import androidx.compose.ui.graphics.Color

object StockAppColors{
    // Superfícies
    val surface0 = Color(0xFFECEAE3) // fundo da página
    val surface1 = Color(0xFFF7F6F2) // fundo da tela
    val surface2 = Color(0xFFFFFFFF) // cards, rows

    // Texto
    val textPrimary = Color(0xFF1A1A1A)
    val textSecondary = Color(0xFF5F5E5A)
    val textMuted = Color(0xFF888780)
    val textSuccess = Color(0xFF27500A)
    val textDanger = Color(0xFF791F1F)
    val textAccent = Color(0xFF0C447C)
    val textWarning = Color(0xFF633806)

    // Fundos semânticos (badges, alertas)
    val bgSuccess = Color(0xFFEAF3DE)
    val bgDanger = Color(0xFFFCEBEB)
    val bgAccent = Color(0xFFE6F1FB)
    val bgWarning = Color(0xFFFAEEDA)

    val border = Color(0xFFE2DFD6)
}